/* global _:false config:false CSL:false */
(function () {
   'use strict';

   angular
      .module('sda')
      .constant('config', config)
      .constant('_', _)
      .constant('CSL', CSL);

})();
